#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../_lz-string@1.4.4@lz-string/bin/bin.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../../_lz-string@1.4.4@lz-string/bin/bin.js" "$@"
  ret=$?
fi
exit $ret
